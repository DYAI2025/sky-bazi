# Debug Changelog — 20260313_121612_694964

**Task:** Add a Near Earth Objects (NEO) tile between `FunnelCta variant="weather"` and `PlanetPositions` components, displaying the top 3 closest flybys for the next 7 days using NASA NeoWs API, with international size comparisons, lunar distance, speed labels, and a safety status badge.

---

## Changes Made

### 1. Added NEO types to `src/services/nasa.ts`
- **File:** `src/services/nasa.ts`
- **Change:** Added `NeoCloseApproach`, `NearEarthObject`, `NeoFeedData` interfaces.
- **Revert:** Remove the three interface declarations.

### 2. Added `fetchNeoFeed()` to `src/services/nasa.ts`
- **File:** `src/services/nasa.ts`
- **Change:** Added `NEO_CACHE_KEY = "neo_v1"`, `NEO_TTL = 6h`, and `fetchNeoFeed()` function.
  - Fetches `/neo/rest/v1/feed` for `today + 7 days`.
  - Flattens `near_earth_objects` (keyed by date) into a single array.
  - Caches result for 6 hours.
  - Throws on non-ok response → component renders null gracefully.
- **Revert:** Remove `NEO_CACHE_KEY`, `NEO_TTL`, and `fetchNeoFeed` function.

### 3. Created `src/components/NearEarthObjects.tsx`
- **File:** `src/components/NearEarthObjects.tsx` (new file)
- **Change:** New React component with:
  - `getSizeComparison(diameterM)` — maps avg diameter to international icon/label:
    `< 10m → 🚌`, `< 30m → 🐋`, `< 100m → 🗽`, `< 300m → 🏟️`, `< 600m → 🗼`, `< 2000m → 🌉`, `else → 🏔️`
  - `getSpeedLabel(kmh)` — maps speed to human label (Vielfache Schallgeschwindigkeit / Schneller als jede Rakete / Extrem schnell)
  - `getSafetyStatus(neo, lunarDist)` — maps `is_potentially_hazardous_asteroid` + lunar distance to color-coded badge (green ✓ / yellow ⚡)
  - `NeoCard` sub-component — displays one NEO: name + date, size 🏟️, distance 🌙, speed 🚀, safety badge
  - Main `NearEarthObjects` component — skeleton loader, sorts neos by lunar distance (ascending), shows top 3 in `grid-cols-1 sm:grid-cols-3`, safety disclaimer footer
- **Revert:** Delete entire file.

### 4. Inserted `<NearEarthObjects>` in `src/App.tsx`
- **File:** `src/App.tsx`
- **Change:** Added `import { NearEarthObjects } from "./components/NearEarthObjects"` and inserted `<NearEarthObjects lang={lang} />` between `<FunnelCta variant="weather" />` and `<PlanetPositions />` in `HomePage`.
- **Revert:** Remove the import and the JSX line.

---

## Notes
- The DONKI endpoints (FLR, GST, CME) show 429/503 errors due to DEMO_KEY rate limits — unrelated to this task. The NEO API fetched successfully (different rate limit bucket) and is now cached for 6 hours.
- Size comparisons use avg of `estimated_diameter_min + max` for a single representative number.
- Safety logic intentionally never shows "red" since CNEOS rarely publishes imminent threats via the public feed. The disclaimer footer reinforces safety.

## Revert Status
- [ ] Change 1 — NEO interfaces (nasa.ts)
- [ ] Change 2 — fetchNeoFeed (nasa.ts)
- [ ] Change 3 — NearEarthObjects.tsx (new file)
- [ ] Change 4 — App.tsx insertion
