# Debug Changelog — 20260313_115118_496094

**Task:** Enhance the Geomagnetic card in SpaceWeather with a live Kp intensity scale showing 4 tiers (Sichtbarkeit + Technik-Check per level).

---

## Changes Made

### 1. Added `lang` prop to SpaceWeather — `src/components/SpaceWeather.tsx`
- **File:** `src/components/SpaceWeather.tsx`
- **Change:** Added `lang: Lang` to `SpaceWeatherProps` interface to support bilingual tier labels.
- **Revert:** Remove `lang` from the props interface.

### 2. Added `KP_TIERS` constant and `getActiveTierIndex()` helper — `src/components/SpaceWeather.tsx`
- **File:** `src/components/SpaceWeather.tsx`
- **Change:** Added a static `KP_TIERS` array (4 entries: Kp 1–4, 5–6, 7–8, 9) with `de`/`en` strings for `label`, `visibility`, and `tech`. Added `getActiveTierIndex(kp: number)` to map `maxKp` → active tier index.
- **Revert:** Remove the `KP_TIERS` constant and `getActiveTierIndex` function.

### 3. Redesigned Geomagnetic card — `src/components/SpaceWeather.tsx`
- **File:** `src/components/SpaceWeather.tsx`
- **Change:** Replaced the old Kp progress bar + storm count text with:
  - Current status line: `● Ruhig · Kp 0` (color-coded, Kp value right-aligned)
  - 4-row intensity scale: each row shows `[Kp range] [Begriff]` and below `[Visibility] · [Tech]`. Active row gets a colored `border-l-2` + subtle translucent background highlight. Inactive rows are muted.
- **Revert:** Restore old geo card body (status indicator → Kp bar → storm count paragraph).

### 4. Updated App.tsx call site — `src/App.tsx`
- **File:** `src/App.tsx`
- **Change:** `<SpaceWeather t={t} />` → `<SpaceWeather t={t} lang={lang} />`
- **Revert:** Remove `lang={lang}` prop.

## Revert Status
- [ ] Change 1 — lang prop (SpaceWeather.tsx)
- [ ] Change 2 — KP_TIERS constant (SpaceWeather.tsx)
- [ ] Change 3 — Geo card redesign (SpaceWeather.tsx)
- [ ] Change 4 — App.tsx call site
