# Debug Changelog — 20260313_125525_738452

**Task:** Add two new subpages — "Erde" (`/erde`) and "Mars-Rover" (`/mars-rover`) — with corresponding header nav links, using NASA EPIC and Mars Rover Photos APIs.

---

## Changes Made

### 1. Added EPIC + Mars Rover API types & fetchers to `src/services/nasa.ts`
- **File:** `src/services/nasa.ts`
- **Change:**
  - Added `EpicImage`, `EpicFeedData` interfaces; `EPIC_CACHE_KEY = "epic_v1"` (1h TTL); `buildEpicImageUrl(image, date)` URL builder; `fetchEpicLatest()` function.
  - Added `MarsCamera`, `MarsPhoto`, `MarsPhotoFeedData` interfaces; `MARS_CACHE_KEY = "mars_latest_v1"` (3h TTL); `fetchMarsLatestPhotos()` function.
  - EPIC URL pattern: `BASE/EPIC/archive/natural/{YYYY}/{MM}/{DD}/jpg/{image}.jpg?api_key=KEY`
  - Mars endpoint: `/mars-photos/api/v1/rovers/curiosity/latest_photos`
- **Revert:** Remove all four interfaces, two cache constants/TTLs, `buildEpicImageUrl`, `fetchEpicLatest`, `fetchMarsLatestPhotos`.

### 2. Added i18n strings to `src/lib/i18n.ts`
- **File:** `src/lib/i18n.ts`
- **Change:** Added `nav.earth`, `nav.mars`, and all `earth.*` / `mars.*` translation keys (DE/EN).
- **Revert:** Remove the 22 new translation entries.

### 3. Created `src/pages/EarthPage.tsx`
- **File:** `src/pages/EarthPage.tsx` (new file)
- **Change:** Earth live viewer using NASA EPIC/DSCOVR satellite.
  - `formatEpicDate()` — parses "2024-03-10 01:15:42" → locale date string
  - `getLongitudeLabel()` — centroid longitude → "46.2° O" format
  - `getOceanLabel()` — rough ocean/continent from longitude
  - Globe icon header with L1 distance fact
  - Full image viewer with prev/next arrow navigation + image counter
  - 3-column info row: timestamp, longitude, L1 distance
  - Horizontal filmstrip with thumbnail buttons (scrollable, gold border on active)
  - Earth fact + source note footer
  - Graceful loading (skeleton circle) and error states
- **Revert:** Delete entire file.

### 4. Created `src/pages/MarsRoverPage.tsx`
- **File:** `src/pages/MarsRoverPage.tsx` (new file)
- **Change:** Mars Rover Curiosity photo feed.
  - `CAMERAS` array defining 7 cameras (ALL, MAST, NAVCAM, FHAZ, RHAZ, MAHLI, CHEMCAM) with DE/EN labels
  - Camera filter pills (only shows cameras present in actual data)
  - Rover status bar with pulsing green dot + Sol counter
  - Featured photo: `aspect-video` hero image with gradient overlay showing camera + date
  - 3-column meta pills: Sol, Earth Date, Camera
  - Click-to-feature photo grid (max 16 photos)
  - Rust accent color `#C1440E` for Mars theming
  - `PhotoCard` sub-component for reusable thumbnails
  - Graceful loading (skeleton) and error states
- **Revert:** Delete entire file.

### 5. Updated `src/components/Header.tsx` — new nav links
- **File:** `src/components/Header.tsx`
- **Change:** Added `onEarth`/`onMars` active-state booleans + two new `<Link>` elements (`/erde`, `/mars-rover`) matching existing style pattern (hidden sm:block, gold when active).
- **Revert:** Remove `onEarth`, `onMars` variables and the two new Link elements.

### 6. Updated `src/App.tsx` — new routes
- **File:** `src/App.tsx`
- **Change:** Added imports for `EarthPage`, `MarsRoverPage`; added `<Route path="/erde">` and `<Route path="/mars-rover">` before the wildcard `*` route.
- **Revert:** Remove two imports and two Route elements.

---

## Debug Notes
- **OVER_RATE_LIMIT (429):** Both pages show graceful error states because `DEMO_KEY` was exhausted (30 req/hr NASA limit across all DEMO_KEY users from this IP). Pages will work with a real `VITE_NASA_API_KEY` from api.nasa.gov (free, 1000 req/hr). This is NOT a code bug.
- **EPIC thumbnail path:** The `/thumbs/` path with `_thumb.jpg` suffix is used for the filmstrip; if thumbnails 404, `onError` falls back to full-resolution URL.
- **Mars camera filtering:** Cameras are filtered client-side from the full latest_photos response. No additional API calls needed per camera change.

## Revert Status
- [ ] Change 1 — EPIC + Mars types/fetchers (nasa.ts)
- [ ] Change 2 — i18n strings
- [ ] Change 3 — EarthPage.tsx (new file)
- [ ] Change 4 — MarsRoverPage.tsx (new file)
- [ ] Change 5 — Header.tsx nav links
- [ ] Change 6 — App.tsx routes
